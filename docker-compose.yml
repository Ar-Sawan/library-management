version: '3.8'

services:
  library-management-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: library_management_app
    ports:
      - "8080:80"
    restart: always
    environment:
      - NODE_ENV=production
